#Had much problem with the compilation with the demult code.

#Looks like the following order of library loading is essential:
-lgzstream -I$(GZSTREAMDIR) -L$(GZSTREAMDIR) 

#As encapsulated in this compile script:
#!/bin/bash
output=$1
g++ -O3 -o ${output/.cpp/.o} $output -lgzstream -I/scratch/hb-1000G_str/local/gzstream -L/scratch/hb-1000G_str/local/gzstream -I/usr/include -std=gnu++11 -lz -Wall

